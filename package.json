{
  "type": "module",
  "scripts": {
    "dev": "nodemon -r dotenv/config src/index.js dotenv_config_path=./.env.dev",
    "start": "node -r dotenv/config src/index.js dotenv_config_path=./.env.prod",
    "docker:dev": "docker compose --env-file .env.dev up"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20"
  }
}
